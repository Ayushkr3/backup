#pragma once
#include <chrono>

//static float GetTimeInMiliSeconds() {
//	std::chrono::high_resolution_clock::time_point now = std::chrono::high_resolution_clock::now();
//	auto elapsed_time = now.time_since_epoch();
//	float t = std::chrono::duration_cast<std::chrono::milliseconds>(elapsed_time).count() / 10; // Divide by 1000 fpr seconds
//	return t;
//}
